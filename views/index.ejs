<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Connect to Platforms</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding-top: 100px;
    }

    .button {
      background-color: #1da1f2;
      color: white;
      padding: 15px 30px;
      font-size: 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin: 10px;
    }

    .button:hover {
      background-color: #0d95e8;
    }

    .youtube {
      background-color: #ff0000;
    }

    .reddit {
      background-color: #ff4500;
    }

    .linkedin {
      background-color: #2867b2;
    }

    .youtube:hover {
      background-color: #cc0000;
    }

    .reddit:hover {
      background-color: #e03a00;
    }

    .linkedin:hover {
      background-color: #1f4a73;
    }
  </style>
</head>
<body>
  <h1>Connect Your Accounts</h1>
  <p>Click the button below to authenticate and access your content.</p>

  <button class="button youtube" id="youtubeButton">Connect to YouTube</button>
  <button class="button reddit" id="redditButton">Connect to Reddit</button>
  <button class="button" id="twitterButton">Connect to Twitter</button>
  <button class="button" id="githubButton" style="background-color: #24292e;">Connect to GitHub</button>

  <button class="button linkedin" id="linkedinButton">Connect to LinkedIn</button>

  <script>
    document.getElementById('youtubeButton').addEventListener('click', function() {
      window.location.href = '/auth';
    });

    document.getElementById('redditButton').addEventListener('click', function() {
      window.location.href = '/auth/reddit';
    });

    document.getElementById('twitterButton').addEventListener('click', function() {
      window.location.href = '/auth/twitter';
    });

    document.getElementById('githubButton').addEventListener('click', function() {
      window.location.href = '/auth/github';
    });

    document.getElementById('linkedinButton').addEventListener('click', function() {
      window.location.href = '/auth/linkedin';
    });
  </script>
</body>
</html>
