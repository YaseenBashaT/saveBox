<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Twitter Liked Tweets</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      color: #333;
      padding: 20px;
    }
    h1 {
      text-align: center;
      color: #1da1f2;
    }
    .tweet-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
    }
    .tweet {
      background-color: white;
      border: 1px solid #ddd;
      border-radius: 10px;
      width: 300px;
      padding: 15px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      transition: transform 0.2s;
    }
    .tweet:hover {
      transform: scale(1.02);
    }
    .tweet p {
      font-size: 14px;
      color: #555;
      margin-bottom: 10px;
    }
    .tweet a {
      display: inline-block;
      padding: 10px 15px;
      background-color: #1da1f2;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
    }
    .tweet a:hover {
      background-color: #0d95e8;
    }
    .no-tweets {
      text-align: center;
      font-size: 18px;
      margin-top: 20px;
      color: #777;
    }
  </style>
</head>
<body>

  <h1>Liked Tweets</h1>

  <div class="tweet-container">
    <% if (likedTweets.length > 0) { %>
      <% likedTweets.forEach(tweet => { %>
        <div class="tweet">
          <p><%= tweet.text %></p>
          <a href="https://twitter.com/i/web/status/<%= tweet.id %>" target="_blank">View on Twitter</a>
        </div>
      <% }) %>
    <% } else { %>
      <p class="no-tweets">No liked tweets available.</p>
    <% } %>
  </div>

</body>
</html>